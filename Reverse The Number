#include<bits/stdc++.h>
using namespace std;

int main(){
#ifdef LOCAL_PROJECT
  	freopen("a.in","r",stdin);
#else
#endif
	ios::sync_with_stdio(0);
	cin.tie(NULL);
	
	int t;
	cin >> t;
	while(t--){
		int a, b=0, c, d;
		cin >> a;
		while(a){
			b=b*10+a%10;
			a/=10;
		}
		cout << b << endl;
	}
}
